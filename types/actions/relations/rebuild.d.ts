/**
 * Rebuild relation function.
 * @function rebuildRelations
 * @param {Boolean} flush Flush intids
 * @returns {Object} Rebuild relation action.
 */
export function rebuildRelations(flush?: boolean): any;
