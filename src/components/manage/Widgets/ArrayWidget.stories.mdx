import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { ArrayWidgetComponent } from './ArrayWidget';
import { injectIntl } from 'react-intl';

<Meta
  title="ArrayWidget"
  argTypes={{
    title: { control: 'text' },
    choices: { control: 'object' },
  }}
  decorators={[
    (Story) => (
      <div style={{ width: '400px' }}>
        <Story />
      </div>
    ),
  ]}
/>

# ArrayWidget

Form field

export const ArrayWidget = (args) => (
  <ArrayWidgetComponent
    id="ArrayWidget"
    title="Array Field"
    choices={[['Foo', 'Foo']]}
    {...args}
  />
);

<Story name="ArrayWidget">{injectIntl(ArrayWidget).bind({})}</Story>

# Props

<ArgsTable of={ArrayWidgetComponent} />
