import React from 'react';
import { Message, Button } from 'semantic-ui-react';

const Edit = (props) => {
  return (
    <Message>
      <Button>Submit</Button>
    </Message>
  );
};

export default Edit;
