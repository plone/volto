Fix blocks_layout synchronization when content has empty blocks fields from migration.

When content has empty blocks={} and blocks_layout={items:[]} (e.g., from migration or automatic Plone generation), adding text to blocks now properly synchronizes blocks_layout with the new block IDs, preventing view breakage. @aryan7081
