Improved screen reader support in `SelectWidget` and `ArrayWidget`. Added dynamic `aria-label` with field name and current value, added keyboard navigation for selected chips, and hide the clear button on required fields. @Wagner3UB