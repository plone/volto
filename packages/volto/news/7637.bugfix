Fixed a bug in Toolbar Types where the "Add Translation…" menu crashed if a language in Plone was not in Volto's language map. Now it shows the language code instead of crashing. Also added defensive fallback logic to prevent SSR crashes when a language is enabled but its locale `.json` file is missing. @pratyush07-hub
