{
  "hooks": {
    "after:bump": "pipx run towncrier build --draft --yes --version ${version} > .changelog.draft && pipx run towncrier build --yes --version ${version}",
    "after:release": "rm .changelog.draft"
  },
  "git": {
    "changelog": "pipx run towncrier build --draft --yes --version 0.0.0",
    "requireUpstream": false,
    "requireCleanWorkingDir": false,
    "commitMessage": "Release @plone/actions ${version}",
    "tagName": "plone-actions-${version}",
    "tagAnnotation": "Release @plone/actions ${version}"
  },
  "github": {
    "release": true,
    "releaseName": "@plone/actions ${version}",
    "releaseNotes": "cat .changelog.draft"
  }
}
