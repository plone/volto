import * as React from 'react';
import Logo from '../Logo/Logo';

const Header: React.FC = () => {
  return (
    <div>
      <div>
        <Logo />
      </div>
    </div>
  );
};

export default Header;
