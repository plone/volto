The main CSS for the Tailwind components now is located in the `@plone/theming` package (`../../theming/styles/main.css`) but it does not have a direct dependency on it. Just use it from the monorepo. @sneridagh
